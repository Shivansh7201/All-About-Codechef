t=int(input())
if(t>40):
    print("YES")
else:
    print("NO")

"""The first line of code takes the input in the integer-type variable name (t) as well as declares it.

Then, in the second line, apply the condition.

Last but not least, we get the output."""
