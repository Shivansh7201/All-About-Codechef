for _ in range(int(input()): """ In this line of python script the user provide the input then onlly the iteration go with the flow"""
  x,c,v,b=map(int,input().split()) """taking input in all the define variable"""
  if((x*c)<=(v*b)): """ applying conditions """
    print("Yes")
  else:                  """Final Output here we get"""
    print("No")
  
